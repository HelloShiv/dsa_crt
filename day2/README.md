# Arrays

array name denotes base aaddress not contains all arrays are constatn pointer.


```c
printf("%c" , "maya"[1]);   // prints a
printf("%c" , "maya"+1);    // prints aya

int a[] = {2,4,2,4,5,6};
cout <<  a;     // address of 1st element       100
cout << &a[0] ; // address fo 1st element       100
cout << &a ;    // address fo complete array    100

cout <<  a+1;     // 104
cout << &a[0]+1 ; // 104
cout << &a +1;  // 120


```

### Q insert a new element in an unsorted array at a praticular position
ans in insertelement.cpp

### Q insert a new element in an sorted array at a paticular position

/*
    
*/